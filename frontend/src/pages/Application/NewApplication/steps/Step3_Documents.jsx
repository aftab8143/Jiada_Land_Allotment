const Step3Documents = ({ data, onChange }) => (
  <div>
    <h2>Step 3: Document Upload</h2>
    <p>Upload required supporting documents (ID proof, business registration, financial statements).</p>
  </div>
);

export default Step3Documents;
