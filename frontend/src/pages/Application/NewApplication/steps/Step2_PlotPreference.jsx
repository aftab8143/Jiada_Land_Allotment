const Step2PlotPreference = ({ data, onChange }) => (
  <div>
    <h2>Step 2: Plot Preference</h2>
    <p>Select preferred zone, plot size, and industry category.</p>
  </div>
);

export default Step2PlotPreference;
