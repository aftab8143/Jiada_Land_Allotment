import PageWrapper from '../../../components/layout/PageWrapper';

const PlotSearch = () => (
  <PageWrapper>
    <h1>Land Bank — Plot Search</h1>
    <p>Search and filter available industrial plots by zone, area, and category.</p>
  </PageWrapper>
);

export default PlotSearch;
